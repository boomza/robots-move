{
  "name": "r",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild random.js --bundle --outfile=dist/bundle.js --platform=node --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hurdlegroup/robotjs": "0.12.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
